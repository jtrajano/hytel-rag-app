# Python dependencies for scripts/
# Install with: pip install -r scripts/requirements.txt

# BigQuery client + pandas integration (for prepare_training_data.py)
google-cloud-bigquery[pandas]>=3.0.0

# GCS client (for future Python scripts that read/write GCS directly)
google-cloud-storage>=2.0.0

google-cloud-bigquery-storage

# Text chunking for RAG pipeline (for chunk_documents.py)
langchain-text-splitters>=0.3.0

# Vertex AI SDK — embeddings (for generate_embeddings.py)
google-cloud-aiplatform>=1.38.0

# Firestore Native Vector Search (for save_to_firestore.py)
google-cloud-firestore>=2.16.0
